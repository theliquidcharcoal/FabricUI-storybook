import {
  DocumentCardTitle as FluentuiDocumentCardTitle,
  IDocumentCardTitleProps,
} from "@fluentui/react";

export const DocumentCardTitle = ({ ...props }: IDocumentCardTitleProps) => {
  return <FluentuiDocumentCardTitle {...props} />;
};
